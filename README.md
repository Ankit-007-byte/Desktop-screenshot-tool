# Desktop-screenshot-tool
This program is a web-based desktop screenshot tool built using HTML, CSS, and JavaScript. It allows the user to capture a screenshot of the entire desktop, a specific window, or a browser tab directly from the browser.
